import { ToolPageShell } from "@/components/tools/tool-page-shell";

export default function CompressToolPage() {
  return (
    <ToolPageShell mode="compress" titleKey="compressTitle" subtitleKey="compressSubtitle" />
  );
}
